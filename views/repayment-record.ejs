<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Repayment</title>
    <style>
        body { 
            font-family: 'Segoe UI'; 
            background: #f9fff9; color: #333; 
            padding: 30px;
        }

        h1 {
            color: #1a7732;
            text-align: center;
        }

        .record {
            border: 1px solid #ddd;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            background: #fff;
        }

        .status {
            font-weight: bold;
        }

        .paid {
            color: green;
        }

        .unpaid {
            color: red;
        }
    </style>
</head>
<body>
    <h1>📋 Your Loan Repayment Records</h1>
    <% if (loans.length === 0) {%>
        <p>You have no loan records yet.</p>
    <%} else {%>
    <% loans.forEach(loan => {%>
        <div class="record">
            <p><strong>Loan Amount:</strong> ₦<%= loan.amount ? loan.amount.toLocaleString() : "N/A" %></p>
            <p><strong>Tenure:</strong> <%= loan.tenure || "N/A" %> days</p>
            <p><strong>Repayment Amount</strong> ₦<%= loan.totalAmount ? loan.totalAmount.toLocaleString() : "N/A" %></p>
            <p><strong>Due Dates:</strong></p>
            <ul>
                <% (loan.dueDates || []).forEach(date => {%>
                    <li><%= date %></li>
                <%}); %>
            </ul>
            <p class="status <%= loan.status %>"><strong>Status:</strong> <%= loan.status %></p>
        </div>
    <%}); %>
    <% } %>

    <%- include("partials/footer") %>
</body>

<script>
    /*document.addEventListener("DOMContentLoaded", () => {
        const loanRecord = JSON.parse(localStorage.getItem("loanRecord"));

        if (!loanRecord) {
            alert("Missing loan data");
            return;
        }
        
        document.getElementById(`loanAmount-${loan.id}`).textContent = loanRecord.loanAmount != null ? loanRecord.loanAmount.toLocaleString() : "N/A";
        document.getElementById("tenure").textContent = loanRecord.tenure || " N/A";
        document.getElementById("interest").textContent = loanRecord.interest != null ? loanRecord.loanAmount.toLocaleString() : "N/A";
        document.getElementById("totalRepayment").textContent = loanRecord.totalRepayment != null ? loanRecord.totalRepayment.toLocaleString() : "N/A";
        const dueDateList = document.getElementById("dueDates");
        dueDateList.innerHTML = "";

        if (Array.isArray(loanRecord.dueDates)) {
            loanRecord.dueDates.forEach(date => {
                const li = document.createElement("li");
                li.textContent = date;
                dueDateList.appendChild(li);
            });
        }

        console.log(JSON.parse(localStorage.getItem("loanRecord")));
    });*/
</script>
</html>